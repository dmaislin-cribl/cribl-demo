id: default
routes:
  - id: VAGP6z
    name: Trim BigJSON
    final: false
    disabled: false
    pipeline: trim_json
    description: Trim Large JSON Events
    clones:
      - index: cribl-modified
    filter: index=='cribl' && sourcetype=='lambda'
    output: default
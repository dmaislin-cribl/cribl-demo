criblIndex:
  type: boolean
  lib: custom
  description: Checks if index is cribl
  args:
    - type: boolean
      name: additional
  value: "index=='cribl' && (additional === undefined ? true : additional)"
  tags: routing
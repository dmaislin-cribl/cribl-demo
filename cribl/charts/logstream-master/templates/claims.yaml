---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-claim
spec:
  accessModes:
    - ReadWriteOnce
  {{ if .Values.config.scName }}
  storageClassName:   {{ .Values.config.scName }}
  {{- end}}
  resources:
    requests:
      storage: 100Mi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: state-claim
spec:
  accessModes:
    - ReadWriteOnce
  {{ if .Values.config.scName }}
  storageClassName:   {{ .Values.config.scName }}
  {{- end}}
  resources:
    requests:
      storage: 100Mi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-claim
spec:
  accessModes:
    - ReadWriteOnce
  {{ if .Values.config.scName }}
  storageClassName:   {{ .Values.config.scName }}
  {{- end}}
  resources:
    requests:
      storage: 100Mi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: groups-claim
spec:
  accessModes:
    - ReadWriteOnce
  {{ if .Values.config.scName }}
  storageClassName:   {{ .Values.config.scName }}
  {{- end}}
  resources:
    requests:
      storage: 1Gi